# Цифровой профпатолог

Основной продуктовый интерфейс находится в `digital_propathologist_mvp` и запускается через Streamlit.

```bash
cd digital_propathologist_mvp
pip install -r requirements.txt
streamlit run app.py
```
